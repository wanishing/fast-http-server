(ns fast-http-server.fast-http-server
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X fast-http-server.fast-http-server/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m fast-http-server.fast-http-server"
  [& args]
  (println "-main with" args))
